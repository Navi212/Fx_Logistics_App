const User = require("../models/user");

const loginUser = () => {
    console.log("Login route visited");
    return;
}

const logoutUser = () => {
    
}

module.exports = {
    loginUser,
    logoutUser
}